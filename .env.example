# Puerto del servidor (Railway lo asigna automáticamente)
PORT=3000

# Host (0.0.0.0 para permitir conexiones externas)
HOST=0.0.0.0

# Entorno de ejecución
NODE_ENV=production

# Ruta base para archivos CSV
CSV_BASE_PATH=./Base de Datos
